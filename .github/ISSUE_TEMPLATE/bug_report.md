---
name: Bug report
about: Something isn't working
labels: bug
---

**Describe the bug**
A clear description of what went wrong.

**To reproduce**
What MCP tool call or sequence triggered the issue?

**Expected behavior**
What did you expect to happen?

**Diagnostics**
Output of `query_logs(level='error', limit=20)` and `list_capabilities()` (redact API keys):

```
paste here
```

**Environment**
- OS:
- Python version (`python --version`):
- unlimited-mcp version / commit:
- Agent CLI and version (e.g. `aider --version`):
